@model Yad2Project.Models.Product
@{
    Layout = null;
}

    <!DOCTYPE html>

    <html>
    <head>
      

        <meta name="viewport" content="width=device-width" />
        <title>Details</title>
    </head>
    <body>

          <div> @Html.EditorForModel()</div>
        <div> @Html.ActionLink("Back to Products", "Index", "Products")</div>

    </body>
</html>
