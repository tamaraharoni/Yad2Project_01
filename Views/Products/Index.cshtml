@model IEnumerable<Yad2Project.Models.VM.ProductVM>
@{
   
    ViewData["Title"] = "Index";
    //Layout = "_Layout.cshtml";

}


@*<h1> Index test</h1>



    <!DOCTYPE html>

    <html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <title>Index</title>
    </head>
    <body>
    *@

        <div>
            <h1>Products</h1>
            @if (ViewBag.User != null)
            {
                <p><span>Hello @ViewBag.User</span></p>
            }

            <button type="button" onclick="location.href='@Url.Action("Create", "Products")'">Add Product</button>
            <button type="button" onclick="location.href='@Url.Action("Login", "Account")'">LogIn</button>

        </div>

        <div>
            @foreach (var item in Model)
            {
                <p>@Html.DisplayFor(model => item.Title)</p>
                @if (item.ImageName != null)
                {

                    <div class="photo-display">

                        <img class="photo-display-img" src="@Url.Action("GetImage", "Products", new { Id = item.Id })" />
                    </div>
                    <div>
                        <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </div>
                }
            }

        </div>
  @*  </body>
    </html>*@
